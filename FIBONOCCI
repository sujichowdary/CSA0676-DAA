#include<stdio.h>
void fib(int n)
{
	int a=0,b=1,c,i;
	for(i=0;i<n;i++)
	{
		printf("%d\t",a);
		c=a+b;
		a=b;
		b=c;
	}
}
int main()
{
	int n;
	printf("enter no of elements:\n");
	scanf("%d",&n);
	fib(n);
}
